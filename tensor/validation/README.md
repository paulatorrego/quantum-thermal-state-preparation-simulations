# N=8 tensor-network validation

This folder contains one compact validation benchmark: MPS quantum trajectories at N=8 compared against the exact Gibbs reference. It is included to document the connection between the tensor-network implementation and the exact calculations in `../exact/`.

The figure is the selected benchmark output. The Julia file is the corresponding trajectory implementation used in the N=8 campaign.
